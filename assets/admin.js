(function(){
    'use strict';
    function selectedProvider(){var selected=document.querySelector('input[name="next_translate_settings[translation_provider]"]:checked');return selected?selected.value:'translatex';}
    function updateProvider(){var provider=selectedProvider();document.querySelectorAll('.nt-provider-panel').forEach(function(panel){panel.hidden=panel.getAttribute('data-provider')!==provider;});document.querySelectorAll('.nt-provider-card').forEach(function(card){var input=card.querySelector('input');card.classList.toggle('is-selected',!!input&&input.checked);});}
    document.addEventListener('change',function(event){if(event.target.matches('input[name="next_translate_settings[translation_provider]"]')){updateProvider();}});
    document.querySelectorAll('.nt-reveal-key').forEach(function(button){button.addEventListener('click',function(){var input=button.parentNode.querySelector('input');var show=input.type==='password';input.type=show?'text':'password';button.textContent=show?nextTranslateAdmin.hide:nextTranslateAdmin.show;});});
    var testButton=document.getElementById('next-translate-test-api');
    if(testButton){testButton.addEventListener('click',function(){var result=document.getElementById('next-translate-test-result');var language=document.getElementById('next-translate-test-language');var provider=selectedProvider();var panel=document.querySelector('.nt-provider-panel[data-provider="'+provider+'"]');var keyInput=panel?panel.querySelector('input[type="password"],input[type="text"]'):null;testButton.disabled=true;testButton.textContent=nextTranslateAdmin.testing;result.textContent='';result.className='';var form=new FormData();form.append('action','next_translate_test_api');form.append('nonce',nextTranslateAdmin.nonce);form.append('language',language?language.value:'es');form.append('provider',provider);form.append('api_key',keyInput?keyInput.value:'');fetch(nextTranslateAdmin.ajaxUrl,{method:'POST',credentials:'same-origin',body:form}).then(function(response){return response.json();}).then(function(data){result.textContent=data.data&&data.data.message?data.data.message:'Unknown response';result.className=data.success?'is-success':'is-error';}).catch(function(error){result.textContent=error.message;result.className='is-error';}).finally(function(){testButton.disabled=false;testButton.textContent=nextTranslateAdmin.test;});});}
    updateProvider();
})();
